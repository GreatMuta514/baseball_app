<h1>パスワードリセット申請</h1>
<%= form_with url: password_resets_url, local: true, methods: :post do |f| %>
	<label for="user_email"><%= "メールアドレス" %></label>
	<%= f.text_field :email, class:'form-control', id:'user_email' %>
	<%= f.submit "送信", class: 'btn btn-primary' %>
<% end %>